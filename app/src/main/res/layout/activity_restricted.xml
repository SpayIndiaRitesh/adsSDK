<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="24dp">

    <TextView
        android:id="@+id/tv_restricted_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Restricted / Ad-Free Screen"
        android:textColor="#202124"
        android:textSize="22sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tv_restricted_desc"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:lineSpacingExtra="4dp"
        android:text="This screen implements 'NoAdsScreen' and is annotated with '@ExcludedFromAppOpen'.\n\nWhen the user navigates here, or puts the app in the background and returns to this screen, App Open Ads and full-screen interstitials are suppressed.\n\nUse this pattern on Login, Payment, Checkout, or Subscription screens."
        android:textColor="#5F6368"
        android:textSize="15sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tv_restricted_title" />

    <Button
        android:id="@+id/btn_back"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_marginTop="32dp"
        android:text="Back to Main Screen"
        app:layout_constraintTop_toBottomOf="@id/tv_restricted_desc" />

</androidx.constraintlayout.widget.ConstraintLayout>
